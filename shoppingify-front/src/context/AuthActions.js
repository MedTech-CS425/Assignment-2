export const LoginStart = (credentials)=>({
    type:"LOGIN_START",
});
export const LoginSuccess = (user)=>({
    type:"LOGIN_SUCCESS",
});
export const LoginStart = ()=>({
    type:"LOGIN_FAILURE",
});